export enum NotFoundMsg {
  header = 'not found header',
  mainText = 'not found main text',
  subtextP1 = 'not found subtext p1',
  linkText = 'not found link text',
  subtextP2 = 'not found subtext p2',
}
